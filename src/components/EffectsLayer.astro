<script type="module">
  import { rainbowCursor } from 'cursor-effects';
  import gsap from 'gsap';

  // Add a rainbow cursor trail
  new rainbowCursor({ length: 20, colors: ['#ff00ff', '#00ffff', '#ffff00'] });

  // Add some random floating animation for the body background
  gsap.to('body', {
    backgroundPosition: '200% 0',
    repeat: -1,
    duration: 10,
    yoyo: true,
    ease: 'linear',
  });
</script>

<style>
  body {
    background: url('/glitter-bg.gif') repeat;
    background-size: cover;
  }
</style>
